name: dbt_red_sox
version: '1.0.0'
profile: dbt_red_sox

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_red_sox:
    +schema: dbt_red_sox  
    staging:
      +materialized: view
